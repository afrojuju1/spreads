job_key: company_valuation_resolve_unresolved:global
job_type: company_valuation_resolve_unresolved
# Optional valuation maintenance lane. Keep disabled unless intentionally running
# company valuation maintenance with a valuation worker.
enabled: false
schedule:
  type: interval
  minutes: 60
payload:
  allow_off_hours: true
  limit_rows: 20000
  batch_cusips: 50
  max_attempts: 5
singleton_scope: resolve_unresolved:global
